# 前端語音功能對比說明

## 📊 現在的實作 vs 使用 az-tts-lib

### 現在的實作（SpeechSynthesis API）

```
使用者點擊發音按鈕
    ↓
前端 JavaScript
    ↓
瀏覽器內建 SpeechSynthesis API
    ↓
直接播放語音
```

**特點：**
- ✅ 完全在前端執行
- ✅ 不需要任何後端呼叫
- ✅ 不需要網路連線（離線可用）
- ❌ 語音品質較差（機械音）

---

### 使用 az-tts-lib 後的實作

```
使用者點擊發音按鈕
    ↓
前端 JavaScript
    ↓
檢查是否有有效的 Token（快取）
    ↓
如果沒有 → 呼叫後端 API 取得 Token
    GET /api/get-speech-token
    ↓
取得 Token 後，使用 Microsoft Speech SDK
    ↓
在前端進行語音合成（產生音訊 Blob）
    ↓
在前端播放音訊
```

**特點：**
- ✅ **主要還是在前端執行**
- ✅ 語音合成在前端完成（使用 Speech SDK）
- ✅ 音訊播放在前端完成
- ⚠️ 需要先取得 Token（從後端 API）
- ⚠️ 需要網路連線（首次或 Token 過期時）
- ✅ 有快取機制（Token 快取 9 分鐘，音訊快取）

---

## 🔍 詳細流程對比

### 現在的流程（SpeechSynthesis）

```typescript
// 完全在前端，同步執行
export const speak = (text: string): void => {
  const utterance = new SpeechSynthesisUtterance(text);
  utterance.lang = "en-US";
  utterance.rate = 0.9;
  window.speechSynthesis.speak(utterance);
  // 完成！不需要任何後端呼叫
};
```

**執行位置：**
- ✅ 100% 前端
- ❌ 0% 後端

---

### 使用 az-tts-lib 後的流程

```typescript
// 主要在前端，但需要先取得 Token
export const speak = async (text: string): Promise<void> => {
  // 步驟 1：取得 Token（可能需要呼叫後端）
  const token = await getValidToken(); // 有快取，不一定每次都呼叫
  
  // 步驟 2：在前端使用 Speech SDK 合成語音
  const blob = await synthesizeToBlob(text, { voice, useCache: true });
  
  // 步驟 3：在前端播放音訊
  playAudioBlob(blob);
};
```

**執行位置：**
- ✅ 95% 前端（語音合成、播放都在前端）
- ⚠️ 5% 後端（僅取得 Token，且會快取）

---

## 📋 關鍵差異

| 項目 | 現在的實作 | 使用 az-tts-lib |
|------|-----------|----------------|
| **語音合成位置** | 前端（瀏覽器 API） | 前端（Azure SDK） |
| **音訊播放位置** | 前端 | 前端 |
| **需要後端呼叫** | ❌ 不需要 | ⚠️ 需要（取得 Token） |
| **後端呼叫頻率** | 0 次 | 很少（Token 快取 9 分鐘） |
| **網路需求** | ❌ 不需要 | ⚠️ 需要（首次或 Token 過期） |
| **離線可用** | ✅ 可以 | ❌ 不可以（需要 Token） |
| **語音品質** | ❌ 較差（機械音） | ✅ 較好（自然） |

---

## 🎯 回答你的問題

### Q: 是否跟現在一樣，都是在前台做讀取音檔的設定？

**A: 是的，主要還是在前端執行！**

**相同點：**
- ✅ 語音合成在前端完成
- ✅ 音訊播放在前端完成
- ✅ 所有使用者互動都在前端處理

**不同點：**
- ⚠️ 需要先從後端取得 Token（但會快取，不是每次都呼叫）
- ⚠️ 需要網路連線（首次或 Token 過期時）

---

## 🔄 實際運作流程

### 第一次播放語音

```
1. 使用者點擊發音按鈕
   ↓
2. 前端檢查：沒有 Token 快取
   ↓
3. 前端呼叫後端 API：GET /api/get-speech-token
   ↓
4. 後端回應 Token（約 100-200ms）
   ↓
5. 前端快取 Token（9 分鐘有效）
   ↓
6. 前端使用 Speech SDK 合成語音（在前端執行）
   ↓
7. 前端播放音訊（在前端執行）
```

**後端參與：** 僅步驟 3（取得 Token）

---

### 後續播放（Token 未過期）

```
1. 使用者點擊發音按鈕
   ↓
2. 前端檢查：有 Token 快取（未過期）
   ↓
3. 直接使用快取的 Token（不呼叫後端）
   ↓
4. 前端使用 Speech SDK 合成語音（在前端執行）
   ↓
5. 前端播放音訊（在前端執行）
```

**後端參與：** 0%（完全不需要後端）

---

### 音訊快取機制

如果播放相同的文字：

```
1. 使用者點擊發音按鈕
   ↓
2. 前端檢查：音訊已在快取中
   ↓
3. 直接播放快取的音訊（不呼叫後端，不重新合成）
   ↓
完成！
```

**後端參與：** 0%（完全不需要後端）

---

## 📝 總結

### 是的，主要還是在前端執行！

**前端負責：**
- ✅ 語音合成（使用 Azure Speech SDK）
- ✅ 音訊播放
- ✅ Token 快取管理
- ✅ 音訊快取管理
- ✅ 所有使用者互動

**後端僅負責：**
- ⚠️ 提供 Token（且會快取，不是每次都呼叫）

**比例：**
- 前端：95%
- 後端：5%（且會快取）

---

## 🎯 對你的影響

### 程式碼層面

- ✅ 主要邏輯還是在前端
- ✅ 不需要大幅修改現有架構
- ✅ 只是多了一個 Token 取得步驟（會自動處理）

### 使用者體驗

- ✅ 語音品質明顯改善
- ⚠️ 首次播放可能稍慢（需要取得 Token）
- ✅ 後續播放很快（使用快取）

### 架構層面

- ✅ 仍然是前端應用
- ✅ 不需要自己的後端服務
- ✅ 只需要使用現有的 Token 服務

---

**結論：主要還是在前端執行，只是多了一個 Token 取得步驟（會自動快取）。**
